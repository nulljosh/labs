# capu

myCapU quick-access dashboard: deep-link shortcuts to Capilano University's Banner portal plus a local GPA/grade tracker (browser localStorage, no server, no stored credentials).

## Usage
Open `index.html` in a browser. Set `targetDate` in the script for a registration/deadline countdown. Add courses to track GPA locally.
