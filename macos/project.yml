name: Quotable
options:
  bundleIdPrefix: com.heyitsmejosh
targets:
  Quotable:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - Quotable
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.heyitsmejosh.quotable.mac
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
