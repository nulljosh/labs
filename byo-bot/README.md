# byo-bot

A rule-based chat bot framework built from scratch in TypeScript, following the [build-your-own-x](https://github.com/codecrafters-io/build-your-own-x) bot guides.

## Run

```
npm install && npm run build && npm start
```

## Next steps

- [ ] Swap rule-based handlers for an LLM-backed handler
- [ ] Add a transport (Slack, Discord, Telegram) instead of stdin/stdout
- [ ] Conversation state / memory across turns
